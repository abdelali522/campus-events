

<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title><%= pageTitle %></title> <%# pageTitle is passed when this partial is included %>

<%# Ensure these CSS paths are correct for your project structure %>
<link rel="stylesheet" href="/css/style.css">
<link rel="stylesheet" href="/css/navbar.css">
<link rel="stylesheet" href="/css/footer.css">

<% if (typeof useFontAwesome !== 'undefined' && useFontAwesome) { %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
<% } %>

<script>
  // Make login status and user info globally available in JavaScript
  const isLoggedIn = <%= locals.isLoggedIn || false %>;
  const currentUser = <%- JSON.stringify(locals.user || null) %>;

 
</script>


